{% extends 'base.html.twig' %}

{% block title %}{{'New question'|trans}}{% endblock %}

{% block body %}
    <h1>{{'Create new Question'|trans}}</h1>

    {{ include('question/_form.html.twig') }}

    <a class="btn btn-secondary" href="{{ path('question_index') }}">{{'back to list'|trans}}</a>
{% endblock %}
