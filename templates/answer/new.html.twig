{% extends 'base.html.twig' %}

{% block title %}{{'New Answer'|trans}}{% endblock %}

{% block body %}
    <h1>{{'Create new answer'|trans}}</h1>

    {{ include('answer/_form.html.twig') }}

    <a class="btn btn-secondary" href="{{ path('answer_index') }}">{{'back to list'|trans}}</a>
{% endblock %}
